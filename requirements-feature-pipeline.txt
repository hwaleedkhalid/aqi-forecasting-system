# Dedicated dependencies for Scheduled Hourly Feature Pipeline (GitHub Actions)
# Preserves Render / Streamlit runtime isolation in requirements.txt
hopsworks[python]==5.0.6
pandas>=2.0.0
numpy>=1.24.0
requests>=2.31.0
python-dotenv>=1.0.0
